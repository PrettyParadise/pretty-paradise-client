server:
  port: 8002
spring:
  application:
    name: pretty-paradise-client

  datasource:
    url: jdbc:mysql://localhost:3306/pretty_paradise
    username: 'root'
    password: 'pp_root'
    driver-class-name: com.mysql.cj.jdbc.Driver

  mail:
    host: smtp.gmail.com
    port: 587
    username: noreplyprettyparadise@gmail.com
    password: pisxvbqiygvipnhu
    properties:
      mail:
        smtp:
          auth: true # authorize this app to use the gmail account given the credentials
          starttls:
            enable: true # starts a TLS connected that the SMTP server expects
